import {createRouter,createWebHistory} from 'vue-router'; import Login from '../views/Login.vue'; import Workspace from '../views/Workspace.vue'; import UserManagement from '../views/UserManagement.vue'; import KnowledgeManagement from '../views/KnowledgeManagement.vue';
const router=createRouter({history:createWebHistory(),routes:[{path:'/login',component:Login},{path:'/',component:Workspace},{path:'/knowledge',component:KnowledgeManagement},{path:'/users',component:UserManagement}]}); router.beforeEach(to=>!localStorage.getItem('token')&&to.path!='/login'?'/login':to.path==='/users'&&localStorage.getItem('role')!=='ADMIN'?'/':true); export default router;
